<Window x:Class="DSI.Desktop.Views.NovaConexaoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DSI.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="Nova Conexão" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Form -->
            <RowDefinition Height="Auto"/> <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Nova Conexão de Dados" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryDarkBrush}"/>
        <Separator Grid.Row="0" VerticalAlignment="Bottom" Margin="0,10"/>

        <!-- Form -->
        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Nome da Conexão *" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" Height="35" VerticalContentAlignment="Center"/>

            <TextBlock Text="Tipo de Banco de Dados *" FontWeight="SemiBold" Margin="0,15,0,5"/>
            <ComboBox ItemsSource="{Binding TiposBancoDisponiveis}" 
                      SelectedItem="{Binding TipoBanco}"
                      Height="35" VerticalContentAlignment="Center"/>

            <TextBlock Text="String de Conexão *" FontWeight="SemiBold" Margin="0,15,0,5"/>
            <TextBox Text="{Binding StringConexao, UpdateSourceTrigger=PropertyChanged}" 
                     Height="80" TextWrapping="Wrap" AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="5"/>
            
            <TextBlock Text="Exemplo: Server=localhost;Database=db;Uid=root;Pwd=123;" 
                       FontStyle="Italic" Foreground="Gray" Margin="0,5,0,0" FontSize="12"/>

            <Button Content="Testar Conexão" 
                    Command="{Binding TestarConexaoCommand}"
                    Style="{StaticResource SecondaryButton}"
                    HorizontalAlignment="Left"
                    Margin="0,10,0,0"
                    Padding="15,5"/>
        </StackPanel>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancelar" 
                    Command="{Binding CancelarCommand}"
                    Style="{StaticResource SecondaryButton}"
                    Width="100" Margin="0,0,10,0"/>
            
            <Button Content="Salvar" 
                    Command="{Binding SalvarCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>
