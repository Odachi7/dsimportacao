<UserControl x:Class="DSI.Desktop.Views.ConexoesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DSI.Desktop.ViewModels"
             Background="{StaticResource BackgroundBrush}">
    
    <UserControl.DataContext>
        <vm:ConexoesViewModel/>
    </UserControl.DataContext>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Conexões" FontSize="32" FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock Text="Gerencie as conexões com bancos de dados" 
                       FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" 
                       Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" 
                    VerticalAlignment="Top" Margin="0,10,0,0">
            <Button Content="➕ Nova Conexão" Style="{StaticResource PrimaryButton}" 
                    Command="{Binding NovaConexaoCommand}" Margin="0,0,10,0"/>
            <Button Content="🔄 Atualizar" Style="{StaticResource SecondaryButton}" 
                    Command="{Binding CarregarConexoesCommand}"/>
        </StackPanel>

        <!-- Lista de Conexões -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" 
                CornerRadius="8">
            <DataGrid ItemsSource="{Binding Conexoes}" 
                      SelectedItem="{Binding ConexaoSelecionada}"
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      GridLinesVisibility="None">
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="2*"/>
                    <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="*"/>
                    <DataGridTextColumn Header="Servidor" Binding="{Binding Servidor}" Width="2*"/>
                    <DataGridTextColumn Header="Banco" Binding="{Binding NomeBanco}" Width="*"/>
                    
                    <DataGridTemplateColumn Header="Ações" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="🔌 Testar" Style="{StaticResource PrimaryButton}"
                                        Command="{Binding DataContext.TestarConexaoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        Margin="5"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
